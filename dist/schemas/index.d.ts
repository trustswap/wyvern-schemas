import { Schema } from '../types';
export declare const schemas: {
    rinkeby: Schema<any>[];
    goerli: Schema<any>[];
    main: Schema<any>[];
    matic: Schema<any>[];
    mumbai: Schema<any>[];
};
